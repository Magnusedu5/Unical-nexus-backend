from django.apps import AppConfig

class CoursesConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'unical_fees_portal.courses'  # Change 'courses' to 'unical_fees_portal.courses'